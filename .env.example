# --- Telegram ---
# Токен бота от @BotFather
BOT_TOKEN=
# Telegram ID администраторов через запятую
ADMIN_IDS=
# Провайдер-токен платежей Telegram (если не используется — оставь пустым)
MONEY_TOKEN=

# --- База данных ---
# ВНИМАНИЕ: целевая СУБД — PostgreSQL (перевод запланирован на ШАГ 4).
# Текущий код пока работает с MySQL.
DB_HOST=localhost
DB_USER=
DB_PASSWORD=
DB_NAME=kwit_bot

# --- Почта (SMTP) ---
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
EMAIL_LOGIN=
# Для Gmail — пароль приложения (App Password), не основной пароль
EMAIL_PASSWORD=
