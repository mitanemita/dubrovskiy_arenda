"""Значения настроек по умолчанию (сидируются в таблицу settings).

Все параметры правятся через бота. Хранятся как строки в settings.value.
"""
from __future__ import annotations

# Ключ -> (значение по умолчанию, человекочитаемое описание)
DEFAULT_SETTINGS: dict[str, tuple[str, str]] = {
    "electricity_tariff": ("14.00", "Тариф за электроэнергию, ₽/кВт·ч"),
    "electricity_coeff": ("1.0", "Коэффициент к стоимости электричества (1.0 = без изменения; напр. 0.93 при УСН)"),
    "penalty_rate": ("0.5", "Ставка пени, %/день"),
    "reminder_days_before": ("2", "За сколько дней до срока напоминать об оплате"),
    "server_cost": ("0.00", "Серверная — фикс. расход в месяц, ₽"),
    "salary_cost": ("0.00", "Зарплаты — фикс. расход в месяц, ₽"),
    "vat_mode": ("none", "Режим НДС: none (без НДС) / vat"),
    "upd_status": ("2", "Статус УПД: 1 или 2"),
}
